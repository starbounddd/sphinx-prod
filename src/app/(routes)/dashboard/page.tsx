import { Typography } from "@/shared/ui/typography";

export default function DashboardPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <Typography size="h2">Dashboard</Typography>
    </div>
  );
}
